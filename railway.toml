# Railway deployment configuration for Verdict Monorepo Root
[build]
builder = "RAILPACK"
buildCommand = "npm run build:backend"

[deploy]
startCommand = "npm run start:backend"
restartPolicyType = "ON_FAILURE"
